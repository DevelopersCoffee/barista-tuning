[package]
name = "edge-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
edge-kernel = { path = "../edge-kernel" }
edge-search = { path = "../edge-search" }
edge-decision = { path = "../edge-decision" }
edge-compiler = { path = "../edge-compiler" }

[lib]
path = "src/lib.rs"

[lints]
workspace = true
