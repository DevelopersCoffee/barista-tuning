name: slm_edge_intelligence
description: Flutter bindings for the Edge Intelligence runtime and offline media intent SDK.
version: 0.2.1
publish_to: none
repository: https://github.com/DevelopersCoffee/barista-tuning
issue_tracker: https://github.com/DevelopersCoffee/barista-tuning/issues

environment:
  sdk: ^3.12.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.4

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.26.0
