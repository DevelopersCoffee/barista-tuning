# Media IR 1.0

Media IR is the immutable domain language for compiled media knowledge packs.
Old schema versions are never edited in place; additive or breaking changes
must create a new version directory.

The runtime treats Media IR as compiled knowledge. User state such as history,
favorites, preferences, downloads, and cache lives outside packs.

